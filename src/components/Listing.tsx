type ListingProps = {
    title: string;
    description: string;
    city: string;
    distance: string;
    price: string;
};

export default function Listing(listing: ListingProps) {
    return (
        <></>
    );
}